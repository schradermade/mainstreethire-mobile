import { colors } from "./theme";

export const textInputThemes = {
  light: {
    inputTextColor: colors.offWhiteFont,
    inputBackgroundColor: colors.primaryLightColor,
    labelColor: colors.darkFont,
    borderColor: colors.borderColorLight,
  },
  dark: {
    inputTextColor: colors.offWhiteFont,
    inputBackgroundColor: colors.primaryColor,
    labelColor: colors.darkFont,
    borderColor: colors.borderColorLight,
  },
};
